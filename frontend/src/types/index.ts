export interface Coordinate {
  x: number;
  y: number;
}

export interface Direction {
  x: number;
  y: number;
}

export interface GameStats {
  score: number;
  steps: number;
  maxScore: number;
}
