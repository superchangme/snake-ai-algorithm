{
  "name": "snake-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
